const app = require('express')();

const PORT =8080;

app.listen(
    PORT,
    () => console.log(rver running on ${PORT})
);

app.get('/veggies', (req, res)=> {
    res.send({
        potato: 2,
        tomato: 12
    })
});
